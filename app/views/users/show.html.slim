p#notice = notice

p
  = @user.login
p
  = image_tag(see_path(@user.logo['grid_id'])) if  @user.logo

=<> link_to '编辑', edit_user_path(@user) ,class: "ui button"
=<> link_to '返回', users_path, class: "ui button"
